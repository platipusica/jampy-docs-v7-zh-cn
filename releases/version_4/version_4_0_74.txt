==============
版本 4.0.74
==============

Jam.py:

* 修复了 :doc:`open </refs/server/item/m_open>` 方法在 order_by 参数为空列表时出错的错误

* 修复了在 :doc:`open </refs/server/item/m_open>` 方法中设置默认顺序且 fields 参数不包含任何默认排序字段时生成的 sql 错误

* 已对使用就地编辑器（inplace editor）时代码中出现的错误进行了异常处理优化，
  以便开发人员能够定位错误原因


管理员:

* 现在，可为打开的模块创建选项卡


演示应用程序:

* 重新设计了搜索目录。
