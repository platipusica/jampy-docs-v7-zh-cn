==============
版本 4.0.78
==============

Jam.py:

* SQLITE 数据库的导入功能现在可用
* 删除了为 SQLITE 数据库创建外部字段索引的操作
* 对带有帮助属性的字段进行了重新设计


管理员:

* 修复了选项卡的相关缺陷


演示应用程序:

* 为客户添加了邮件发送功能。
  它演示了如何使用 :doc:`server </refs/client/abstr_item/m_server>` 方法从客户端模块执行服务器模块中定义的脚本，以及如何通过带有虚拟表（virtual_table）的数据项的字段来创建数据输入表单。
